apiVersion: v1
kind: Service
metadata:
  name: lab6-backend
  labels:
    app: lab6-web-app
    component: backend
spec:
  type: ClusterIP
  selector:
    app: lab6-web-app
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
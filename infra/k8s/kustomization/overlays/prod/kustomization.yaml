apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lab6-data

resources:
  - ../../base
  - namespace.yaml
  - secret.yaml

patches:
  - path: patch-statefulset.yaml

labels:
  - includeSelectors: true
    pairs:
      environment: prod